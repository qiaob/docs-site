# docstate

The Docstate site: publish Markdown and interactive HTML as versioned documents
that remember state. See the [repository README](https://github.com/qiaob/docs-site)
for everything.

```bash
pip install "docstate[postgres]"
docstate serve
```
