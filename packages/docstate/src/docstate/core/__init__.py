"""The domain: models, text rules, publishing use cases, rendering, import
rules and pre-publish checks. Nothing in here knows about databases, HTTP or
templates; storage adapters and delivery layers depend on this package, never
the other way round."""
